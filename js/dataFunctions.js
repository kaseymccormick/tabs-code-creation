//get the objects from the programData.js

//grab an object's name

//gram an objects description

// grab the objects formats > the array then dig into array


// /---------------------------------------
// Utility Functions
//---------------------------------------
//adds a space between words
function space(x) {
    return x.replace(/([a-z])([A-Z])/g, "$1 $2");
}